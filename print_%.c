#include "main.h"

/**
 * print_% - print %.
 * @my_list: arguments.
 * Return: count of chars.
 */

int print_%(va_list my_list)
{
	int num_chara_print = 0;

	write(1, &c, 1);
	num_chara_print++;

	return (num_chara_print);
}
